{
  "name": "get_product_sameness_report",
  "description": "Returns sameness analysis for given elements using tolerances and intersecting geometry (mirrors MCPToolHelper logic).",
  "inputSchema": {
    "type": "object",
    "properties": {
      "element_ids": {
        "type": "array",
        "items": {
          "type": "string"
        }
      }
    },
    "required": [
      "element_ids"
    ]
  }
}