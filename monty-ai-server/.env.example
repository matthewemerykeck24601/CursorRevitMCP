# Required for chat
XAI_API_KEY=

# Default model (xAI Grok — pick what your key supports)
XAI_MODEL=grok-3-latest

PORT=8787

# Reject /api/chat and /api/auth/session without Authorization: Bearer (Monty sends your APS access token)
MONTY_REQUIRE_BEARER=1
