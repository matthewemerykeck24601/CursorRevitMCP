<Window x:Class="EDGE.TicketTools.HardwareDetail.Views.HWEditTitleblockWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:EDGE.TicketTools.HardwareDetail.Views"
        Height="300" Width="350" Title="Select Titleblock" ResizeMode="NoResize" Background="LightGray">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="15"/>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="15"/>
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="10"/>
      <RowDefinition Height="60"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="40"/>
      <RowDefinition Height="10"/>
    </Grid.RowDefinitions>
    <Grid Column="1" Row="1">
      <Label Content="Select New Titleblock" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Top"/>
      <Viewbox HorizontalAlignment="Left" VerticalAlignment="Bottom" MaxHeight="35" MaxWidth="315"
               Stretch="Uniform">
        <TextBox Name="currentTitleBlockLabel" Text="Current Titleblock: " FontSize="14" IsReadOnly="true"
                 TextWrapping="Wrap" HorizontalAlignment="Left" VerticalAlignment="Bottom" Background="LightGray"
                 BorderBrush="LightGray" BorderThickness="0" Width="315"/>
      </Viewbox>
    </Grid>
    <Grid Column="1" Row="2">
      <ListBox SelectionChanged="titleblockListBox_SelectionChanged" x:Name="titleblockListBox" Grid.Column="1"
               Grid.Row="2" Width="315" Height="150" HorizontalAlignment="Center" VerticalAlignment="Center"
               DisplayMemberPath="ValueString" SelectedValuePath="ValueString"
               ItemsSource="{Binding titleblockList}" SelectedValue="{Binding SelectedTitleBlock}"/>
    </Grid>
    <Grid Column="1" Row="3">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <Button x:Name="selectButton" Grid.Column="0" Content="Select" Height="30" Width="150"
              HorizontalAlignment="Center" Command="{Binding EditTitleblockCommand}"/>
      <Button Click="cancelButton_Click" x:Name="cancelButton" Grid.Column="1" Content="Cancel" Height="30"
              Width="150" HorizontalAlignment="Center"/>
    </Grid>
  </Grid>
</Window>